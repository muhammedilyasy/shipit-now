{
  "name": "shipit-checker",
  "version": "1.0.0",
  "description": "Cloudflare Worker to check GitHub contribution status",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
